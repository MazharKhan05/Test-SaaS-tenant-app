import Span from "../Core/Span";
import { DonationCategoriesData } from "@/lib/types/websiteManagement/page/components/block/data";

export const DonationCategories = ({
  data,
}: {
  data: DonationCategoriesData;
}) => {
  return (
    // <div className="w-full grid grid-cols-3 col-span-3">
    //   {data && data.map((cat) => <Span data={cat} />)}
    // </div>
    <></>
  );
};
